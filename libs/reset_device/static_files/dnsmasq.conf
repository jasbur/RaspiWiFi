interface=wlan0
dhcp-range=10.0.0.10,10.0.0.15,12h
#dhcp-option=option:router,10.0.0.1
#dhcp-option=option:netmask,255.255.255.0

# Delays sending DHCPOFFER and proxydhcp replies for at least the specified number of seconds.
dhcp-mac=set:client_is_a_pi,B8:27:EB:*:*:*
dhcp-reply-delay=tag:client_is_a_pi,2


address=/#/10.0.0.1

# Start a DHCP server to be used alongside hostapd. Reply to all DNS queries
# with our IP address - this allows us to intercept traffic and redirect it to
# the captive portal.
# dnsmasq --interface=wlan0 --address=/#/10.0.0.1 --dhcp-range=10.0.0.10,10.0.0.15,12h
# TODO: generate hostapd.conf
# hostapd -dd /tmp/hostapd.conf